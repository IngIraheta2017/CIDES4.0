{% extends 'base1.html.twig' %}

{% block body %}
<section id="container" >
    <!--main content start-->
    <section id="main-content">
        <section class="wrapper">
            <!--state overview start-->
            <!-- Aqui incrustamos el codigo que generamos del comando de crud -->

  <div class="container">
    <h1>Editar Menu</h1>
    {{ form_start(edit_form) }}
    <div class="form-group">
      {{ form_widget(edit_form.nombremenu, { 'attr' : {'class' : 'form-control', 'placeholder' : 'Nombre Menu'} }) }}
      <span class="text-danger">{{ form_errors(edit_form.nombremenu) }}</span>
  </div>
  <div class="form-group">
     {{ form_widget(edit_form.parent_menu, {'attr': {'class': 'form-control', 'style':'resize:horizontal', width:'200%' }}) }}
    <span class="text-danger">{{ form_errors(edit_form.parent_menu) }}</span>
      <br/>
      <br/>
        <input type="submit" value="Editar"  class="btn btn-info"/>

    {{ form_end(edit_form) }}
</div>
    <ul>
        <li>
            <a href="{{ path('menuitem_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete"  class="btn btn-danger">
            {{ form_end(delete_form) }}
        </li>
    </ul>
            <!-- Aqui termina la inscrustacion del codigo -->
            </div>
        </section>
</div>
      </div>

  <div class="modal-footer">
        CIDES 2017
  </div>
        </div>

    </div>
</div>

<!-- Modal Observacion -->






{% endblock %}
